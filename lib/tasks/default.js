'use strict';

module.exports = function(gulp) {
    /**
     * Default task startet with `gulp`.
     *
     * Starts {@link dev}.
     *
     * @public
     * @namespace default
     */
    gulp.task('default', ['dev']);
};
